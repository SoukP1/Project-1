import pandas as pd 

def historical_data_cleaner(historical_data):
    for symbol in tickers:
        symbol == ticker
        ticker = historical_data[historical_data["symbol"] == ticker].drop("symbol", axis=1)
    
    clean_portfolio = pd.concat([ticker], axis=1, keys=[ticker])

    return clean_portfolio 

